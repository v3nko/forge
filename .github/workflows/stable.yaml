name: Stable images

on:
  push:
    tags:
      - "v[0-9]*.[0-9]*.[0-9]*"

permissions:
  contents: read
  packages: write

jobs:
  publish:
    uses: ./.github/workflows/_images.yaml
    with:
      lane: stable
      images: all
      version: ${{ github.ref_name }}
